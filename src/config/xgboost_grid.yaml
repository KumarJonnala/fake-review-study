# TF-IDF is fitted only on the training fold.
# Each combination is evaluated on the validation set.
hyperparameters:
  n_estimators: [200, 400, 800]
  max_depth: [3, 5, 7]
  learning_rate: [0.03, 0.10]
  subsample: [0.8, 1.0]
  colsample_bytree: [0.8, 1.0]
  min_child_weight: [1, 3]
  reg_lambda: [1, 5]
