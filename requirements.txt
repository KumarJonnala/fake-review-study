# Core data / classical ML  (src/{data,vectorizer,evaluation}.py, xgboost + svm trainers)
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
scipy>=1.12
xgboost>=2.0
PyYAML>=6.0
joblib>=1.3

# Synthetic review generation and validation
# (src/generate_synthetic_reviews.py, src/validate_generated.py)
# Both are imported DIRECTLY by those two scripts. They also happen to arrive transitively
# via transformers and accelerate, which is why they went unlisted -- but neither script
# imports transformers, so a generation-only environment without these lines breaks.
requests>=2.31
tqdm>=4.66

# BERT fine-tuning  (src/bert_classifier/train.py)
# accelerate is not imported by name; transformers.Trainer refuses to run without it.
transformers>=4.40
datasets>=2.18
torch>=2.2
accelerate>=0.28

# RAG classifier  (src/rag_classifier/RAG.py)
# sentence-transformers backs HuggingFaceEmbeddings and faiss-cpu backs the FAISS vector
# store; neither is imported by name, and both are required at runtime.
sentence-transformers>=2.7
langchain-core>=0.2
langchain-community>=0.2
langchain-huggingface>=0.0.3
langchain-ollama>=0.1
faiss-cpu>=1.8
